plugins {
    id 'java'
    id "org.sonarqube" version "3.3"
}

group 'com.mathiric'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0'

    testImplementation("org.assertj:assertj-core:3.20.2")
}

test {
    useJUnitPlatform()
}

sonarqube {
    properties {
        //property "sonar.projectKey", "richard-mathis-sonarsource_myproduct"
        //property "sonar.organization", "richard-mathis-sonarsource"
        //property "sonar.host.url", "https://sonarcloud.io"

        property "sonar.projectKey", "myproductd"
        property "sonar.name", "myproductd"
        property "sonar.organization", "richard-mathis-sonarsource"
        property "sonar.host.url", "http://localhost:9000"
        property "sonar.login", "3db3b5fda5655f4bdaeb5c44f90f13d3dc8923ac"
    }
}

